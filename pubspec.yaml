name: image_labelling_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.5 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_mlkit_image_labeling: ^0.7.0
  google_mlkit_text_recognition: ^0.8.1
  image_picker: ^0.8.7+5
  flutter_speed_dial: ^7.0.0
  go_router: ^8.0.1
  tflite: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/